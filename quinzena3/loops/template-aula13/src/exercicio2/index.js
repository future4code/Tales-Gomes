/* const vetorNumeros = [11, 15, 18, 14, 12, 13]
let maiorAtual = 0


for(let index= 0; index < vetorNumeros.length ; index++) { // 0 < 7; 1 < 7
        if(vetorNumeros[index] > maiorAtual) {
        maiorAtual = vetorNumeros[index]
     }
}

console.log("O maior número é ", maiorAtual)
 */

// Exercício teste

/* const lista = [10, 11, 12, 15, 18, 19, 21, 23, 25, 27, 30] // array com número que vão ser usado pelo if
for (let numero of lista) { // O for vai usar o if pra tentar encontrar no array quais vão ser os números maiores que 18
  if (numero > 18) { 
		console.log(numero) // Aqui vai ser imprimido os número que serão maiores 
                //19, 21, 23, 25, 27, 30
	}
} */

// B) Para acessar o resultado delas, tu tem que inspecionar elementos e ir em console


// Desafio 1

/* const quantidadeTotal = Number(prompt("Digite a quantidade de linhas: "))
let quantidadeAtual = 0
while(quantidadeAtual < quantidadeTotal){
  let linha = ""
  for(let asteriscos = 0; asteriscos < quantidadeAtual + 1; asteriscos++){
    linha += "0"
  }
  console.log(linha)
  quantidadeAtual++
} */
 // Ele vai criar 4 linhas, cada uma delas vai conter uma quantidade de 0 correspondendo a linha
