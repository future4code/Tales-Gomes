import React from "react";
import Typography from '@material-ui/core/Typography';

const PageTitle = (props) => {
    return <Typography variant="h1"Typography>{props.title}</Typography>
}

export default PageTitle;